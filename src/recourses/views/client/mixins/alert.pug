mixin alert-success(time) 
    - if (messages.success)
                div(
                    class = "alert alert-success"
                    data-time = time,
                    show-alert
                )
                    span= messages.success

mixin alert-empty(count)
    - if (count < 1)
        h1 Danh sách rỗng!
         